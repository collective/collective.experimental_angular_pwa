import { Component } from '@angular/core';
import { ViewView } from '@plone/restapi-angular';

@Component({
  selector: 'newsitem',
  templateUrl: 'newsitem.html'
})
export class NewsitemComponent extends ViewView {
}
